# Contrat API (OpenAPI)

Le contrat API est maintenu dans [`openapi.yml`](../../openapi.yml) à la racine du dépôt.

## Mise à jour

À chaque évolution d'endpoint :

1. Modifier `openapi.yml`
2. Valider le fichier (lint OpenAPI — _à venir en CI_)
3. Inclure la mise à jour dans la PR associée

## Statut

_À venir — voir issue `docs(openapi): add API contract`._
